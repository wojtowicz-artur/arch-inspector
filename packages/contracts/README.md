# @arch-inspector/contracts

Dependency-free TypeScript helpers for declaring module boundaries and typed
query, command, and event contracts. The inspector reads these declarations
statically from `module.arch.ts` files; it never imports application code.
